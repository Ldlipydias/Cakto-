{
  "entities": {
    "NotificationHistory": {
      "title": "NotificationHistory",
      "description": "A record of a generated fake notification.",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The ID of the user who generated the notification."
        },
        "value": {
          "type": "number",
          "description": "The value of the PIX generated."
        },
        "createdAt": {
          "type": "timestamp",
          "description": "When the notification was generated."
        }
      },
      "required": ["userId", "value", "createdAt"]
    }
  },
  "firestore": {
    "users/{userId}/notifications/{notificationId}": {
      "schema": { "$ref": "#/entities/NotificationHistory" },
      "description": "History of notifications generated by the user."
    }
  }
}
